name: Client CI

on:
  pull_request:
    paths:
      - 'client/**'
  push:
    paths:
      - 'client/**'
  merge_group:

jobs:
  ci:
    name: Run Client CI
    runs-on: ubuntu-latest
    steps:
      - name: Run Client CI (sleep)
        run: sleep 60
